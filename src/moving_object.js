class MovingObject {
    constructor(options) {
        const { pos, vel } = options
        this.pos = pos;
        this.vel = vel;
    }

    draw = () => {
        
    }
}

module.exports = MovingObject;